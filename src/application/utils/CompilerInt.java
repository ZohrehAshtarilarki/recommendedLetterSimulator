package application.utils;

import application.model.Faculty;
import application.model.Recommendation;

public interface CompilerInt {
	String compile(Faculty faculty, Recommendation recommendation);
}
